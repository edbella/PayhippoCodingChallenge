const MuiButton = {
  root: {
    "&$disabled": {
      cursor: "not-allowed",
      pointerEvents: "auto",
    },
    borderRadius: 5,
    height: "fit-content",
    width: "fit-content",
    fontSize: "1rem",
    fontWeight: 400,
  },
}

export default MuiButton;
